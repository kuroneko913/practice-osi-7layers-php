# OSI参照モデルを実装して理解する
OSI参照モデルを実装して理解するためのプロジェクトです。

## セットアップ

```bash
make
```

## 実行
受信側を先に起動しておく
```bash
make receiver
```

送信側を起動し、送りたいビット列などを標準入力から送信
```bash
echo "01010101" | make sender
```

受信側にビット列が1bitずつ表示されることを確認できる
